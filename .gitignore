# Secrets
.env
.env.*

# Python
__pycache__/
*.pyc
*.pyo

# Editors (no trailing slash: also matches the null-device placeholders)
.idea
.vscode
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Shell/home dotfiles (accidentally committed)
.bash_profile
.bashrc
.profile
.zprofile
.zshrc
.gitconfig
.gitmodules
.ripgreprc
.mcp.json

# Claude Code / editor config
.claude/

# Git bare repo internals (accidentally committed)
HEAD
config
hooks
objects
refs

# Ebook build trees (regenerable via make ebooks)
build/
